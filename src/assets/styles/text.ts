import {StyleSheet} from 'react-native';

export const text = Object.freeze(
  StyleSheet.create({
    textCenter: {
      textAlign: 'center',
    },
    textLeft: {
      textAlign: 'left',
    },
    underline: {
      textDecorationLine: 'underline',
    },
  }),
);
